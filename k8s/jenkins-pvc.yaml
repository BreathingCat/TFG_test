apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: jenkins-pv-claim
  namespace: pruebas
spec:
  storageClassName: jenkins-pv1
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 15Gi